/**:
  ros__parameters:
    plugin_denylist:
      # common

      # extras
      - image_pub
      - vibration
      - distance_sensor
      - rangefinder
      - wheel_odometry
      # ─── heavy management plugins we want to disable ──
      - mission          # MISSION_REQUEST / item upload
      - rallypoint       # RALLY_FETCH / upload
      - geofence         # FENCE_FETCH / upload
      - waypoint         # legacy waypoint interface (if present)

    # plugin_allowlist:
    #   - 'sys_*'
